// "??? ????? ???? ??? ???? ??? ???"
// "??? ?? ???? ???? ????? ??? ??? ???? ??? ??? ?? ?????? ??? ????? ?? ??? ????"

// LINK : https://codeforces.com/group/Rilx5irOux/contest/638270/problem/D
#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ld long double
#define pii pair<int,int>
#define pll pair<ll,ll>
#define PI acos(-1)
#define Ones(n) __builtin_popcountll(n)
#define MSB(n) (63 - __builtin_clzll(n))
#define LSB(n) (__builtin_ctzll(n))
#define mem(arrr, xx) memset(arrr,xx,sizeof arrr)
#define fr first
#define sc second
#define pb push_back
#define all(a) a.begin(),a.end()
#define allr(a) a.rbegin(),a.rend()
#define no cout<<"NO\n"
#define yes cout<<"YES\n"
#define imp cout<<"IMPOSSIBLE\n"
#define nl '\n'
#define sp ' '
#define ENG_GAMAL ios_base::sync_with_stdio(false); cin.tie(nullptr); cout.tie(nullptr);
using namespace std;
// ????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????????
const int N = 2e5 + 5, M = 1e3, LOG = 20, inf = 0x3f3f3f3f;
ll infLL = 0x3f3f3f3f3f3f3f3f;

void TC() {
    int n;
    cin >> n;
    map<string, vector<pair<ll, string>>> m;
    for(int i=0; i<n; ++i) {
        string s;
        cin >> s;
        int ind;
        for(int j=0; j<s.length(); ++j) {
            if(s[j] == '-') {
                ind = j;
                break;
            }
        }
        string name = s.substr(ind + 1);
        string id = s.substr(0, ind);
        m[name].push_back({stoll(id), id});
    }
    for(auto& [name, x]: m) {
        sort(x.begin(), x.end());
    }
    for(auto &[name, vec]: m) {
        int s = vec.size();
        for(int i=0; i<s; ++i) {
            cout << vec[i].second << "-" << name << "\n";
        }
    }

}
void file()
{
#ifndef ONLINE_JUDGE
    freopen("Input.txt", "r", stdin);
    freopen("Output.txt", "w", stdout);
    freopen("Error.txt", "w", stderr);
#endif
}

int main() {
    file();
    ENG_GAMAL
// test-independent code ??????????????????????
// ????????????????????????????????????????????
    ll tc = 1;
//     cin >> tc;
    while (tc--)
    {
        TC();
    }

    return 0;
}